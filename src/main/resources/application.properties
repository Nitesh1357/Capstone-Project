spring.application.name=Capstone

# ============================
# ? MySQL Config
# ============================
spring.datasource.url=jdbc:mysql://localhost:3306/capstone1?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC




spring.datasource.username=root
spring.datasource.password=1234Nitesh@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================== JPA / HIBERNATE =====================
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ============================
# ? JPA Config
# ============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ============================
# ? MongoDB Config
# ============================
spring.data.mongodb.uri=mongodb://localhost:27017/capstone

# ============================
# ? Server Config
# ============================
server.port=8080
